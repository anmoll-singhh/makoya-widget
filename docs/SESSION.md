# Makoya — Session & Phase Tracker (living doc)

> Purpose: keep context across long sessions. Update at the end of every work block.
> Owner: Claude (building + selling this). Founder: not building — strategy/approvals/keys only.

## Locked strategy (2026-06-22)
- **Positioning:** Honest hybrid (real scan + remediation guidance + monitoring + honest preferences widget). NOT overlay-compliance.
- **Customer:** SMB self-serve (PLG via public scanner funnel).
- **Budget:** Bootstrap / OSS-first (<$50/mo + usage until revenue).
- **Goal:** Demo-first (~2 wks end-to-end loop), then harden for real customers.

## Ground truth (what actually exists — supersedes CLAUDE.md / MASTER_CHECKLIST, which are STALE)
- ✅ Widget: 15 features, i18n (en/es/fr/de), 9 profiles, Shadow DOM, loader/core split, mobile bottom-sheet. Deployed.
- ✅ Scanner: Playwright + @axe-core/playwright, WCAG 2.0/2.1/2.2 A+AA, 6 custom DOM checks, second-pass verify, fallback, screenshot, multi-page. (lib/scanner)
- ✅ Supabase backend (NO mock mode left), RLS, auth (@supabase/ssr), admin gating via ADMIN_EMAILS.
- ✅ Client dashboard: customizer-first, live-preview iframe, debounced autosave, report tab.
- ✅ Admin CRM: customer overview, plan field, consultations/leads, cron rescan route.
- ✅ Plain-language issue translation (lib/scanner/plain-language.ts).
- ❌ Billing (no Stripe/Lemon Squeezy). ❌ Email (no Resend). ❌ Scalable queue/monitoring. ❌ AI remediation. ❌ PDF export. ❌ Sentry/PostHog/CI.
- ⚠️ TECH DEBT: lib/shared is a hand-maintained MIRROR of packages/shared (will drift). Scanner runs in request path (won't scale to monitoring).

## Roadmap phases
- **Phase 0 — Foundation (no keys needed): ✅ DONE 2026-06-22.** Stale docs rewritten (CLAUDE.md, MASTER_CHECKLIST). Shared-config drift killed: canonical=packages/shared, generated mirror via scripts/sync-shared.mjs, CI drift test lib/shared-sync.test.ts. Root `npm run ci` = sync+typecheck(web+widget)+tests. GitHub Actions .github/workflows/ci.yml. Observability seam lib/observability.ts (env-guarded no-op). Verified: 27 tests pass, typecheck 0. (Sentry/PostHog SDK install deferred to a keyed block.)
- **Phase 1 — Revenue loop (demo core): ✅ LIVE & verified end-to-end 2026-06-23 (against email STUB).** Deployed to Vercel prod (makoya-gamma.vercel.app). Verified live: `/scan` 200; `POST /api/public-scan` runs the REAL Lambda browser scan (example.com → 80/100 + plain-English issues); `POST /api/scan-ingest` → lead row in live `leads` table (correct shape/defaults) + stub email recorded; row matches what `/admin/leads` reads. Only-typecheck (not click-tested): the `/admin/leads` UI render itself (auth-gated, needs browser login) — but its data source is verified.
  - ✅ `leads` table migration (service-role only, RLS no-policy) — supabase/migrations/20260622210000_leads.sql
  - ✅ Email seam: lib/email/* — provider interface + STUB provider (records to outbox, no send) + honest report-email builder (no compliance claims). Resend swaps in lib/email/index.ts only.
  - ✅ lib/leads.ts (createLead/listLeads, service-role).
  - ✅ POST /api/scan-ingest (public, CORS, validation, rate-limit) → lead + stub email. + OPTIONS.
  - ✅ Fixed scanner-integration/EmailCapture.tsx copy (removed "signed compliance file"/"ADA lawsuits" → honest).
  - ✅ Unit tests (report-email honesty guardrail, leads mapper). 33 tests green, typecheck 0.
  - ✅ Public scan API `POST /api/public-scan` (nodejs, maxDuration 60, NO auth). SSRF double-gate: new `lib/scan-utils/public-url.ts#isPublicHttpUrl` (pure, unit-tested) + existing `validateScanUrl`; per-IP rate limit (5/min); calls `runScan` directly (NOT stored — public scans are ephemeral); returns {score, totals, topIssues[], finalUrl}; generic error + machine `code` only (no SSRF oracle).
  - ✅ Public scanner page `/scan` (client): URL input → scan → big score/100 + critical/serious/moderate/minor breakdown + top-5 plain-English issues → email-capture card → `POST /api/scan-ingest` → "Report on its way" success. shadcn UI, honest copy, loading/error states, mobile-friendly.
  - ✅ Admin Leads page `/admin/leads` (server, getAdminUser-gated): `listLeads(getAdminSupabase())` → shadcn Table (email, domain, score, issue total, status, date), worst-first sort (lowest score then most issues), empty state. "Leads" + "Requests" links added to admin nav (app/admin/layout.tsx).
  - ✅ Unit test for the SSRF guard: `lib/scan-utils/public-url.test.ts` (10 cases). CI green: 43 tests, typecheck 0.
  - ✅ BLOCKER CLEARED (2026-06-23): leads migration APPLIED to Supabase via MCP + verified live (table exists, RLS enabled, 0 policies = service-role-only by design, 3 indexes; smoke insert/read/delete round-trips the exact lib/leads.ts shape incl. defaults + jsonb totals). Stack merged to main (local, unpushed). Still pending live: the real browser scan only runs on Vercel (@sparticuz/chromium = Lambda), so the FULL /scan→/api/scan-ingest→lead→/admin/leads loop is verified end-to-end only after a Vercel deploy (gated on founder approval).
  - ✅ PDF report export SHIPPED + deployed + QA'd live (2026-06-23): "Download PDF" on /scan → POST /api/report-pdf (@react-pdf/renderer, server-only, no SSRF surface, strict input caps, honesty-guardrail unit test). Browser-verified: click → 200, real PDF, no console errors. Also fixed a latent bug (bare-domain input → 400) by sending result.finalUrl to both /api/report-pdf AND /api/scan-ingest; consolidated host helpers into lib/utils/url.ts.
  - 🔶 TODO next: swap stub→Resend (needs RESEND_API_KEY + verified domain); durable cross-instance rate-limit; resolved-IP SSRF check (current guard is hostname-string only — DNS-rebinding still needs network egress controls). Minor known debt: severity label/color maps + score-verdict thresholds duplicated across /scan UI and the PDF (different CSS-vs-hex representations).
- **Phase 2 — Money:** **Stripe** checkout + webhook (signature-verified + idempotent) → plan; server-side plan gating. [needs: Stripe account, test mode ok. Stripe ≠ Merchant-of-Record → founder owns tax/VAT.]
- **Phase 3 — Demo polish: 🔶 IN PROGRESS.** ✅ Honest-hybrid landing page at `/` SHIPPED + deployed + QA'd live (2026-06-23): hero with inline scan deep-link (`/scan?url=` → auto-runs), cited why-honest stat cards as proof (category facts, no competitor named), how-it-works, three surfaces, transparency block, final CTA. Research-informed (last30days + 2026 conversion sources). Browser-verified end-to-end (hero input → auto-run → results in ~12s, no console errors); **our own scanner scores the landing 100/100** (AA contrast fix applied after review). Copy guarded by a unit test (no compliance/guarantee claims, no named competitor). Spec: docs/superpowers/specs/2026-06-23-landing-page-design.md. Remaining Phase 3: Book-a-call (founder's OWN booking system — placeholder embed slot now, real code later; NOT Calendly); PostHog funnel dashboard (project needed).
- **Phase 4 — V1 hardening:** Inngest queue; scheduled monitoring + "score dropped" alerts; Claude AI remediation suggestions (human-confirmed); WordPress plugin.
- **Phase 5 — V2/Enterprise:** agency/white-label portal; productized human-audit (partner); VPAT/ACR; SSO; CI/CD axe gate.

## Phase H — Bulletproofing (founder directive 2026-06-24/25: make everything built solid/trustworthy/industry-grade, no gaps/loopholes, before new features)
- **H-DB (✅ DONE 2026-06-25):** Ran Supabase security advisors. Hardened `create_default_site_config` (was a SECURITY DEFINER trigger helper callable by anon+authenticated via `/rest/v1/rpc/` with a mutable search_path) → pinned search_path + revoked execute; verified anon/auth can no longer execute. Confirmed `leads` + `consultation_requests` RLS-no-policy are service-role-only BY DESIGN (not bugs). Migration: `supabase/migrations/20260625000000_harden_create_default_site_config.sql`.
- **H1 Test depth (✅ DONE):** 106 → **212 tests** (added 73 env-free incl. email-failure-doesn't-block-lead, scanner scoring determinism/order-invariance, PDF input caps, url helpers, admin roles edge, provider seam). Playwright E2E for the public funnel + guarded RLS cross-tenant isolation suite (skips without creds). `@vitest/coverage-v8` coverage gate (floor below current).
- **H2 API security (✅ DONE + LIVE):** resolved-IP/DNS-rebinding SSRF defense in `public-scan` (rejects loopback/private/link-local/CGNAT/metadata after DNS lookup + post-redirect recheck; pure IP classifier, 26 unit cases) + **Zod** validation at every API boundary (generic safe errors, no field leakage). Deployed + live-QA'd: example.com→87 still works; localhost / 169.254.169.254 / malformed / bad-JSON all → 400 (no 500).
- **H3 CI security+quality (✅ DONE, runs on push):** GitHub Actions for Gitleaks (secrets), Semgrep OSS (SAST), CodeQL, Dependabot (npm+actions), Lighthouse CI (**a11y=1.0 hard-fail**), axe dogfood on our own `/` + `/scan`, npm audit (warn), ESLint+Prettier (non-blocking legacy).
- **H-remaining:** founder enables Supabase **leaked-password protection** (HaveIBeenPwned) — Auth settings toggle, needs dashboard (no MCP/token). Founder creates free-tier accounts for Sentry (DSN) / PostHog / Upstash Redis (durable rate-limit) / Inngest, then I wire them. Rotate the Anthropic + Resend keys pasted in chat.
- **PAID options for true industry-grade (founder decides):** Vercel Pro ($20/mo, WAF/DDoS) · Supabase Pro ($25/mo, PITR backups) · Snyk/Socket.dev · BrowserStack/LambdaTest (cross-browser widget) · one-time pro pen-test (~$1–5k) · Stripe Tax (+0.5%/txn).

## Required from founder (access)
Anthropic API key (rotate) · Resend key (rotate; domain verified) · **Stripe** account (test mode ok; founder owns tax — Stripe ≠ MoR) · Sentry + PostHog projects · Upstash Redis + Inngest (free tiers) · own booking-system embed code (later) · enable Supabase leaked-password protection (Auth toggle) · confirm Supabase service-role.

## Session log
- 2026-06-25 (block 10): **Phase 1 widget licensing gate — built multi-agent, on its own branch (founder-gated for ship).** Founder asked how to run the widget on Shopify/WordPress and how to stop free/backdoor use → established the model: the widget JS is copyable, so **protect the license, not the code** — gate at the public config endpoint (the chokepoint every visitor's browser hits). Wrote `docs/plans/PHASE-1-LICENSING.md`; a **Plan-agent review** caught real holes and the plan was revised v2: cache must be `no-store` (the earlier `s-maxage=300 + swr=86400` would let a suspended site work for ~a day + risked per-origin cache poisoning), **fail-OPEN on infra error / fail-CLOSED on not-found**, dropped the unnecessary FIRST_PARTY carve-out (live-preview inlines config server-side, never hits the endpoint), keep `active` a loader-local transport flag (not in `@makoya/shared` → no drift-gate trip). **Honest threat model:** Origin-locking = deterrent (spoofable via server-side proxy); direct-`core.js` embed bypasses the gate entirely — both deferred to **Phase 1.5 (signed tokens + core gate)**. Founder then **deleted ALL `sites`** (clean slate; `leads`/auth untouched) to onboard fresh — so `createSite` now seeds `allowed_domains` (apex+www) at insert, not just the migration backfill. **Multi-agent build (2 disjoint lanes, no shared files):** Lane A backend (migration `20260626000000_widget_licensing`, `getSiteLicense`, endpoint rewrite, `logWidgetGate` seam, 13 route tests) + Lane B widget (loader respects `active:false`; network failure still mounts on defaults; 5 tests). `npm run ci` green (236 tests, typecheck 0, shared-sync clean). **Collision handled:** a 2nd Claude session was editing the *same* main checkout (its `7fab09f feat(scanner)` commit landed on my branch); I committed ONLY my 8 lane files, left its WIP untouched; it then moved to its own worktree `makoya-scanner-v3`/`feat/scanner-evidence-v3` and `main` got fast-forwarded to `7fab09f`. Rebased my work cleanly via reset-to-main + cherry-pick → `feat/phase-1-licensing` = `main` + exactly one commit (`bd62c28`), CI re-verified green on the scanner-inclusive base. **Founder-gated before ship (NOT done): (1) apply `widget_licensing` migration to prod Supabase** (auto-mode classifier denied unattended apply — correct, prod DDL needs explicit OK), **(2) merge branch→main** (would make main non-deployable until the migration lands). Enforcement ships OFF (`WIDGET_ENFORCE` unset = monitor mode). Expert (Plan-agent) final verification: **GO**, 0 blocking issues, `npm run ci` exit 0 (236 tests), clean fast-forward, migration⇄code consistent, deploy hazard confirmed (createSite writes `allowed_domains` → migration must precede deploy). **Founder authorized full rollout → SHIPPED:** applied `widget_licensing` migration to prod Supabase (3 cols verified: `license_status` default 'active', `trial_ends_at`, `allowed_domains` default '{}'), fast-forwarded `main` → `e54b684`, pushed origin, deployed `vercel --prod` (`dpl_9Qpw1etTxRXtapJ7VbbsKkQ3y1mc`, READY, makoya-gamma). **Verified live:** `/api/config/<unknown>` → 200, `Cache-Control: no-store`, `active:true` (monitor mode, nothing blocked), safe fields only; `/` → 200 (app healthy). Enforcement OFF (`WIDGET_ENFORCE` unset). NEXT (founder chose "decide later"): Phase 1.5 signed-token wall + close direct-`core.js` bypass BEFORE flipping `WIDGET_ENFORCE`; then Shopify/WordPress delivery (install docs + WP plugin); optional: wire widget loader test into CI.
- 2026-06-25 (block 9): **Phase H bulletproofing — multi-agent, executed + verified live.** Founder directive: make everything built solid/industry-grade, free tools used directly, paid listed for approval. **DB security (founder-approved):** hardened `create_default_site_config` (search_path + revoke rpc exec — closes advisor 0011/0028/0029), verified. **Multi-agent (3 isolated worktrees, strictly disjoint file lanes to honor the agent-board rule):** Lane A = SSRF resolved-IP/DNS-rebinding + Zod at all API boundaries; Lane B = CI security/quality pipeline (Gitleaks, Semgrep, CodeQL, Dependabot, Lighthouse a11y=1.0, axe-dogfood, coverage gate, eslint/prettier); Lane C = +73 tests (email-no-block-lead, scoring determinism, PDF caps, etc.) + Playwright E2E + guarded RLS isolation. Pre-provisioned shared deps (zod, @vitest/coverage-v8, @playwright/test, eslint, prettier) on main FIRST so no agent touched package.json (no lockfile conflicts). Merged all 3 into main with **zero conflicts**; integrated `npm run ci` green (**106→212 tests**, 6 RLS skipped without creds, typecheck 0). **Deployed + live-QA'd on prod:** example.com→87 still scans; localhost / 169.254.169.254 / malformed-body / invalid-JSON all → 400 (no 500). Consolidated the memory/Stripe/Phase-H docs onto `main` (they'd been stranded on `feat/dashboards-ui-wip` — the source-of-truth split was itself a gap). Remaining Phase H = founder-gated (leaked-password toggle; Sentry/PostHog/Upstash/Inngest free accounts; key rotation). NEXT: clean up agent worktrees + merged branches; wire monitoring once accounts exist; then Phase 2 (Stripe).
- 2026-06-24 (block 8): **Merged + deployed scanner-v2; re-planned for Stripe + own-booking.** Verified-before-merge: clean auto-merge into `main` (`ac82c9e`, 0 conflicts), full `npm run ci` green (106 tests); prod `scans` already had `score_breakdown`+`engine_meta` columns (migration `scan_evidence_columns` applied+tracked) so authed dashboard `saveScan` won't break; public `/scan` ephemeral (unaffected). Deployed `vercel --prod` (dpl_7QD4ri…, READY); verified live example.com→87/100 v2 deterministic model. Founder decisions: billing=**Stripe** not Lemon Squeezy (Stripe ≠ MoR → founder owns tax); **dropped Calendly** → founder's own booking system (placeholder embed now). Backed up all 16 branches to origin earlier this day.
- 2026-06-24 (block 7): **Built the central memory system** (founder was losing track across 2-3 parallel Claude sessions). Two parts: (1) **`docs/STATUS.md`** — a glanceable dashboard (tables: at-a-glance / live-in-prod / in-flight / blocked-on-founder / up-next + roadmap one-liners) with a first-class **agent-coordination board** mapping each worktree+branch to its owner/lane + "rules of the board" (one worktree = one branch = one agent; never edit outside your lane; deploy only from clean `main`) — directly targets the past failure where the scanner agent mutated the main checkout. Also lists the ~13 stale branches to prune. STATUS.md carries the **update protocol** (update STATUS first, then SESSION, then CLAUDE.md; mark ✅ only when verified). (2) **gbrain** (local PGLite brain) installed + initialized + MCP-registered (`✔ Connected`, user scope, binary `C:\Users\ANMOL\.bun\bin\gbrain.exe`). Embeddings **disabled** (no OpenAI/Voyage/ZeroEntropy key on this Windows box) → full-text search only; semantic search is a one-key upgrade later. Seeded `makoya-status-dashboard` + `makoya-project-state-2026-06-24`; put→search round-trip verified. CLAUDE.md now opens with a "🧠 Project memory — READ THIS FIRST" section + `## GBrain Configuration` / `## GBrain Search Guidance` blocks. Note: gbrain's `gstack-gbrain-install` printed a false "install may be broken" (Windows path quirk in its version check) — the binary is fine (v0.42.52.0). Doctor reports `unhealthy` only due to disabled embeddings + empty-brain `resolver_health` (vacuous); functionally healthy. **Locked strategy update:** backend + features first behind a minimal beautiful demo frontend, THEN redo the whole frontend strategically (research-led, deliberate placement). Considered claude-mem (founder re-asked) — recommended NOT adding it: gbrain+PGLite already fills the role and is lighter (no Redis/Postgres/daemon), and two memory systems reintroduce the exact confusion we're killing. NEXT: founder decides the two in-flight merges (scanner-v2, dashboard UI) + branch pruning; then backend push (Phase 2 billing needs Lemon Squeezy; Phase 4 hardening).
- 2026-06-23 (block 6): **Resend email wired — stub→real, verified live end-to-end.** Founder supplied a Resend key (domain `mailer.jewlx.ai`, verified/sending-enabled — confirmed via Resend API). Implemented `lib/email/resend-provider.ts` (fetch-based, no SDK dep; send failures return `ok:false` and never throw so the already-persisted lead is never blocked) and flipped `lib/email/index.ts` to return it when `RESEND_API_KEY` is set (else stub). Added `RESEND_API_KEY` + `EMAIL_FROM` (`Makoya <reports@mailer.jewlx.ai>`) to `lib/env.ts`, local `.env.local`, and Vercel **production**. 4 new unit tests (success / non-2xx / network-reject); `npm run ci` green (63 tests, typecheck 0). Committed on a branch → fast-forwarded to `main` (577ca24). **Verified live:** a direct Resend send delivered (msg id returned), and the full funnel `POST /api/scan-ingest` on prod returned `{ok:true, emailed:true, provider:"resend"}` with a lead row (test lead cleaned up after). ⚠️ Founder shared the key in chat — should rotate. **Deploy note:** the main working dir was being concurrently mutated by the scanner agent's process (still writing scanner-v2 phase-2 UI/route work into the main checkout despite the worktree split), so I deployed from an isolated clean `main` worktree (`makoya-deploy`, removed after) to ship reviewed code only. **This rolled scanner-v2 OFF prod** — prod now runs clean `main` (widget fix + provisioning + Resend); scanner-v2 stays committed/isolated on `feat/scanner-trustworthy-v2` (worktree `C:\Users\ANMOL\Desktop\makoya-scanner`, commit d1b6162) until founder blesses it. NEXT: tell the scanner agent to run from the scanner worktree (its process is still in the main dir); decide scanner-v2 merge; then Phase 2 (Lemon Squeezy) / Phase 3 (Calendly + PostHog).
- 2026-06-23 (block 5): **Cleared the two open issues; deployed + verified live (multi-agent).** (1) **Empty dashboard fixed:** prod `sites` had 0 rows (verified) → provisioned a site for anmols@wavesmvmnt.com (owner 501e558f…, domain `wavesmvmnt.com`, plan free, site id `0a1cd598-4dfd-4e61-9728-1aa58bbb2e91`) via Supabase MCP after founder authorization. The `trg_create_default_site_config` trigger auto-created the `site_config` row (note: table is **`site_config`** singular, not `site_configs`) with all 15 features enabled, bottom-right, sensible defaults — confirmed via live `/api/config/<siteId>`. Data prerequisite for the customizer + report tab is met (UI render itself is auth-gated, not browser-clicked this block). (2) **Widget scroll-chaining fixed:** dispatched a worktree subagent to add `overscroll-behavior: contain;` to both `.mky-panel` scroll containers (desktop L129 + mobile L642) in `packages/widget/src/ui/styles.ts`, rebuild, and copy bundles into `apps/web/public/widget`. Merged the branch into main `--no-ff`, cleaned up the worktree, re-ran `npm run ci` on main (59 tests green, typecheck 0). Note: the widget CSS isn't minified, so the rule ships as `overscroll-behavior: contain` (with a space), present 2× in the bundle. (3) **Admin email:** founder chose to set it explicitly — replaced prod `ADMIN_EMAILS` = `anmols@wavesmvmnt.com,creativesgpt@wavesmvmnt.com` (anmols primary; `isAdmin` treats the list as a set, so order is cosmetic — both are equal admins). (4) **Deployed** `vercel --prod` from apps/web (dpl_HA46LskdEYidWqvWd6myWnUziqWu, READY, aliased makoya-gamma.vercel.app). **Verified live:** prod `/widget/core.js` contains the overscroll rule 2×; `/api/config/0a1cd598…` returns the new site's config. NEXT: founder browser-login QA (customizer renders + admin/CRM access for anmols); then continue roadmap (Resend swap pending key; Phase 2 billing hard-blocked on Lemon Squeezy account).
- 2026-06-23 (block 4 hotfix): Landing replaced the old `/`→`/dashboard` redirect, so signed-in users hit the marketing page and thought the app/CRM/customizer were gone (nothing was deleted — dashboard/admin/customizer code untouched, both still 307→/login when anonymous). Fix: `/` now redirects authenticated users to /dashboard (server-side getUser, mirrors dashboard layout); anonymous traffic still sees the landing. Deployed + verified (anon /=200 landing, /dashboard gated, CI 59 green).
- 2026-06-23 (block 4): **Shipped the honest-hybrid landing page at `/`** (Phase 3 demo polish), research-first. Ran `/last30days` (engine ran Reddit-only — no X auth on this box — so leaned on 2026 web-conversion sources) → distilled: value clear in <3s, free tool in the hero, one value-loaded CTA repeated, cited stats as proof, docs-as-product trust. Brainstormed + speced (docs/superpowers/specs/2026-06-23-landing-page-design.md), built a static Server Component landing (lib/landing-copy.ts pure copy + guardrail test; HeroScanInput client island; /scan now reads ?url= via Suspense+useSearchParams and auto-runs). One review-found defect fixed (text-neutral-400 failed AA contrast → neutral-500). `npm run ci` green (59 tests, typecheck 0). Merged to main, deployed, **QA'd live**: `/` serves the landing, hero deep-link auto-runs the scan (~12s, no console errors), and our own scanner scores the landing 100/100. Decisions this block: category contrast (no competitor named), no pricing table (free-scan CTA), category-fact stats as proof (no fabricated logos). Next Phase-3 items need founder input: Calendly link (book-a-call), PostHog project (funnel analytics). Phase 2 (Lemon Squeezy billing) still hard-blocked on a Lemon Squeezy account.
- 2026-06-23 (block 3): **Shipped PDF report export** (demo next-step) end-to-end with a multi-agent flow. TDD: lib/pdf/report-content.ts (pure, honesty-guardrail tested) → ReportDocument.tsx (branded A4, built-in fonts) → render-report.ts → public /api/report-pdf (no SSRF, body/issue caps, rate limit) → "Download PDF" on /scan. 12 new tests; `npm run ci` green (55 tests, typecheck 0). Ran react-best-practices review (clean — react-pdf is server-only, not in client bundle) + a 3-angle security/correctness/conventions code review: fixed one real bug (raw bare-domain input → 400; now uses finalUrl, which also repaired the pre-existing email gate) and consolidated 4 host-extraction copies into lib/utils/url.ts. Merged to main, deployed (vercel --prod), and **QA'd live in a real browser**: scan → results → Download PDF → 200 real PDF, no console errors. Background agent produced docs/MAKOYA_OVERVIEW.pdf (founder visual overview: real screenshots + diagrams, honest copy). Stored ANTHROPIC_API_KEY in apps/web/.env.local (gitignored) — FOUNDER SHOULD ROTATE (was shared in chat). Plugins: installed claude-md-management; declined claude-mem (heavy infra). NEXT PHASE: Phase 2 (Lemon Squeezy billing) is hard-blocked on a Lemon Squeezy account; unblocked alternative is Phase 3 demo polish (honest landing page) — awaiting founder pick.
- 2026-06-23 (block 2): Resumed as CTO. Read all tracking docs (no re-audit). **Corrected a bad handoff assumption: the Supabase MCP was NOT connected** — it was registered (local scope) with a stale arg format (positional token `sbp_…` instead of `--project-ref=vgxvkegjnibvsngjrxqa` + `SUPABASE_ACCESS_TOKEN` env). Re-registered it correctly via `claude mcp`; now shows ✔ Connected. **MCP tools load only at session start, so the migration is applied in the NEXT session (founder chose: restart → apply via MCP).** Verified via Supabase Management API that the `leads` table does NOT exist yet and the access token is valid (HTTP 200). **Plugins evaluated** (founder ask): installed official `claude-md-management` (lightweight: `claude-md-improver` skill + `/revise-claude-md`, reinforces doc hygiene); **declined `claude-mem`** (heavy — needs Redis+Postgres+worker daemon; contradicts bootstrap ethos and duplicates the git-tracked doc-based memory). **Decisions locked:** (1) apply migration via MCP after restart; (2) then merge BOTH Phase 0 + Phase 1 to main (CI-green, additive); deploy stays gated on founder approval. Then (post-restart, same block): **applied the leads migration via Supabase MCP and verified it live** (RLS-no-policy as designed; smoke insert/read/delete OK), ran `npm run ci` (43 tests / typecheck 0, green), and **merged the Phase 0 + Phase 1 stack into `main`** with a `--no-ff` merge commit. Then (founder approved deploy): pushed main to origin and ran `vercel --prod` (dpl_G6Whk4ibo31J53e8v5N3oyjL2g43, READY, aliased makoya-gamma.vercel.app — note: NO GitHub auto-deploy; production is manual `vercel --prod` from apps/web). **Verified the full funnel loop LIVE** (real scan → ingest → lead in DB; test rows cleaned up). Phase 1 is now ✅ LIVE. NEXT: swap email stub → Resend (needs RESEND_API_KEY + verified domain), then PDF report export (@react-pdf/renderer, no keys).
- 2026-06-23: Phase 1 funnel made demo-visible end-to-end. Added public scan API (/api/public-scan, SSRF double-gate + rate limit, ephemeral, generic errors), public scanner page (/scan, score + breakdown + plain-English issues + email gate → /api/scan-ingest), admin Leads page (/admin/leads, worst-first, getAdminUser-gated, service-role read), + isPublicHttpUrl guard with 10 unit tests. `npm run ci` green: 43 tests / typecheck 0. NOT verified live: browser scan needs Lambda chromium (cannot run on Windows); leads loop still blocked on applying the leads migration to Supabase.
- 2026-06-22: Audited real codebase (corrected stale docs). Ran competitive teardown (FTC, Overlay Fact Sheet, WebAIM, accessiBe pages) → docs/research/COMPETITIVE_TEARDOWN.md. Founder locked strategy (honest hybrid / SMB / bootstrap / demo-first). Delivered 3-perspective product walkthrough. **Executed Phase 0** (docs, shared-config drift guard, CI, observability seam) — verified green (27 tests, typecheck 0). Changes uncommitted on main. NEXT: commit Phase 0 (branch first); get Resend key + domain to start Phase 1 (scanner→email→lead loop).
