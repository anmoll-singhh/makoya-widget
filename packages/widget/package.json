{
  "name": "@makoya/widget",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:core": "vite build",
    "build:loader": "cross-env BUILD_TARGET=loader vite build",
    "build": "npm run build:core && npm run build:loader",
    "dev": "vite",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "typescript": "^5.9.3",
    "vite": "^5.4.21"
  }
}
